name: 67

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Replace secrets in test.html
        run: |
          sed -i "s|__BOT_TOKEN__|${{ secrets.BOT_TOKEN }}|g" test.html
          sed -i "s|__CHAT_ID__|${{ secrets.CHAT_ID }}|g" test.html
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          publish_branch: gh-pages